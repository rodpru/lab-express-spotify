<h1>album</h1>
<div class="container d-flex flex-wrap">

{{#each albums}}
            <div class="card" style="width: 18rem;">
                <img src="{{images.[2].url}}" class="card-img-top" alt="...">
                <div class="card-body">
                    <h5 class="card-title">{{name}}</h5>
                    <a href="/tracks/{{id}}" class="btn btn-primary" name="tracks">Tracks</a>
                </div>
            </div>
{{/each}}
</div>
